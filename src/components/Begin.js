import { Button, Box } from "@chakra-ui/react";

const Begin = () => {

  return (
    <Box p={2}>
      <Button size="lg" bg="gray.100" color="blue.500">
        Begin Your Journey
      </Button>
    </Box>
  );
};

export default Begin;
